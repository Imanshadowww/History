# Upsun configuration, generated using AI at: 2026-09-10T16:41:13.302Z
# AI can make mistakes. Please modify this file to suit your needs.
applications:
  rvg:
    type: php:8.5
    build:
      flavor: none
    hooks:
      build: |
        set -ex
        composer install --no-progress --no-interaction --optimize-autoloader --no-dev
    web:
      locations:
        /:
          root: ''
          passthru: /index.php
          allow: true
    variables:
      php:
        memory_limit: 256M
        max_execution_time: 300
        upload_max_filesize: 10M
